{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --filter \"./artifacts/mockup-sandbox\" --if-present run build",
    "typecheck": "pnpm -r --filter \"./artifacts/mockup-sandbox\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
